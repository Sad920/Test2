# Matrix Multiplier

Simple C++ console program that multiplies two matrices up to 10x10. Enter matrix sizes and values, then press `m` to multiply or `q` to quit.

## Build and run

```bash
g++ -std=c++17 -O2 -o matrix_app main.cpp
./matrix_app
```
